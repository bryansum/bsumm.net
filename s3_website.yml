s3_id: <%= ENV['S3_ID'] %>
s3_secret: <%= ENV['S3_SECRET'] %>
s3_bucket: bsumm.net

cloudfront_distribution_id: E2P46SEP3GVA5O
cloudfront_wildcard_invalidation: true
cloudfront_invalidate_root: true

max_age:
  "css/*": 3600 # 1 hour: 60s * 60m * 1h
  "images/*": 31449600 # 1 year: 60s * 60m * 24h * 7d * 52w
  "*": 300

gzip: true
